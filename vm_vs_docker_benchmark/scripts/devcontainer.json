{
    "name": "DOOM en Docker",
    "build": {
      "dockerfile": "Dockerfile",
      "context": "."
    },
    "settings": {},
    "extensions": [],
    "forwardPorts": [5000],
    "postCreateCommand": "echo Contenedor listo para ejecutar Doom",
    "remoteUser": "vscode"
  }
  
  Le dice a VS Code cómo construir el contenedor usando Dockerfile.
  
  Abre el puerto 666 (opcional para juegos/servicios que lo necesiten).
  
  Ejecuta un comando al final solo para mostrar que todo funcionó.